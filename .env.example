# Local development. Copy to .env and adjust if needed.
DATABASE_URL=postgresql://custody:custody@localhost:5432/custody
PORT=4000

# Where the evidence files live. Leave unset to use the pack vendored in this
# repository at ./testdata, which is what a deployed service should do.
# EVIDENCE_ROOT=/absolute/path/to/custody-testdata
