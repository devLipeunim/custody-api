node_modules/
.env
.env.local
*.log

# Generated, never committed: 30MB of random bytes standing in for a phone
# extraction. Recreate with testdata/scripts/make-large-file.sh or, on a
# deployed service, npm run deploy:setup.
testdata/evidence/case-c/handset-extraction.bin
testdata/.pristine/

# Built documents: the deck, the write-up and their Word versions. Generated
# from scripts/deck-content.js and scripts/writeup-content.js, which are the
# source. Rebuild with npm run deck, deck:pptx, writeup and docx.
deck/
